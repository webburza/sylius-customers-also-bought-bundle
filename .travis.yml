language: php

cache:
    directories:
        - vendor

php:
    - 7.0
    - 5.6
    - 5.5

before_install:
    - composer self-update

install:
    - composer install --no-interaction --prefer-dist

script:
    - vendor/bin/behat
